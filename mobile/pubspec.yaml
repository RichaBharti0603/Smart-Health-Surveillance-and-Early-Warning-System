name: mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  smooth_page_indicator: ^1.2.1
  url_launcher: ^6.2.6   # ✅ Correct place
  http: ^1.2.2           # ✅ Fixed indentation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # Uncomment when you actually have assets
  # assets:
  #   - assets/images/
  #   - assets/icons/
